getOrders:
	without page, limit => 200
	
    without limit =>  200
    without page => 200
    
    page with minus => 400
    limit with minus => 400

    page with alpbhabet=> 400
    limit with alpbhabet=> 400

    page in decimal=> 400
    limit with decimal => 400

    valid page number, limit => 200

    accessive page number=> 200


update order:
    
    without order_id =>  405
    without status => 400

    with invalid order id => 404
    with alpbhabet order id=> 400
    with minus order id => 400
    decimal order id
    
    with invalid status (i.e without TAKEN)
    Order already TAKEN status

Create Order
    without any parameter :400
    without origin parameter: 400
    without destination parameter: 400
    origin parameter not as array: 400
    destination parameter not as array: 400
    origin parameter array with only latitude: 400
    origin parameter array with extra parameter: 400
    destination parameter array with only latitude: 400
    destination parameter array with extra parameter: 400
    origin parameter array with invalid latitude: 400
    origin parameter array with invalid longitude: 400
    destination parameter array with invalid latitude: 400
    destination parameter array with invalid longitude: 400
    
    with valid origin and destination parameter